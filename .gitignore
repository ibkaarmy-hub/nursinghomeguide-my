# ── Credentials & secrets ─────────────────────────────────────
token_sheets.json
token_*.json
client_secret*.json
*.env
.env
.env.*
config.py
secrets.py
.secrets/
_gsc/

# ── Apify raw data (large, not needed in repo) ────────────────
*_raw.json
apify_results.json
apify_input.json
photos_results.json
firecrawl_results.json
run_*.json
run_response.json
kl_selangor_run_id.txt
rescrape_input.json
rescrape_results.csv
rescrape_queries.txt
.rescrape_cache/

# ── Working CSVs (intermediate merge stages) ─────────────────
sheet.csv
sheet_*.csv
kl_selangor_slugs.json
photo_*.json
photo_files_list.txt
audit_data.json
mdac_*.csv
_tmp/audit_places_cache.json

# ── Downloaded photos ─────────────────────────────────────────
photos/

# ── Python cache ──────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Claude Code worktrees (must never be committed as submodules) ──
.claude/worktrees/

# ── OS / editor ───────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
credentials_temp.json
_enrich_cache/
_scrape_cache/
audit_triage.html
